{
  "name": "afro-node-frontend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "lint": "eslint . --ext js,jsx --report-unused-disable-directives --max-warnings 0",
    "preview": "vite preview"
  },
  "dependencies": {
    // Add your actual frontend dependencies here if they exist
  },
  "devDependencies": {
    // Add your actual frontend dev dependencies here if they exist
  }
}
